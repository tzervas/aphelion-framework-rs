[package]
name = "aphelion-core"
version = "0.1.0"
edition = { workspace = true }
license = { workspace = true }
authors = { workspace = true }
repository = { workspace = true }

[features]
default = []
# Ecosystem integrations - feature flags defined but deps commented until ready
burn = []
cubecl = []
rust-ai-core = []

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
tracing = { workspace = true }
sha2 = { workspace = true }
hex = { workspace = true }

aphelion-macros = { path = "../aphelion-macros" }

# Ecosystem dependencies (uncomment when ready)
# burn = { version = "0.16", optional = true }
# cubecl = { version = "0.4", optional = true }
