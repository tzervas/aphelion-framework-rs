[package]
name = "aphelion-core"
version = { workspace = true }
edition = { workspace = true }
license = { workspace = true }
authors = { workspace = true }
repository = { workspace = true }
description = "Core library for Aphelion AI pipeline framework - graphs, configs, pipelines, backends, diagnostics"
keywords = { workspace = true }
categories = { workspace = true }
readme = "../../README.md"

[features]
default = []
# Ecosystem integrations
burn = []
cubecl = []
rust-ai-core = ["dep:rust-ai-core", "dep:candle-core"]
cuda = ["rust-ai-core", "rust-ai-core/cuda"]
tritter-accel = []
tokio = ["dep:tokio"]

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
tracing = { workspace = true }
sha2 = { workspace = true }
hex = { workspace = true }
tokio = { workspace = true, optional = true }

aphelion-macros = { version = "1.2.0", path = "../aphelion-macros" }

# Ecosystem dependencies
# burn = { version = "0.16", optional = true }
# cubecl = { version = "0.4", optional = true }
rust-ai-core = { workspace = true, optional = true }
candle-core = { workspace = true, optional = true }
